
    <main>
        {{#if hasProducts}}
        
       <h1>My products </h1>
         {{#each prods}}
           <p><b> {{this.title }} </b></p>
         {{/each}}
         {{ else }}
         <h1>No products found</h1>
         {{/if}}
    </main>
