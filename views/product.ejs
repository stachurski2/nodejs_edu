<head>
    <%- include('includes/head.ejs') %>
</head>
<body>
    <%-  include('includes/navigation.ejs') %>
    <main class="centered">

        <% if( product) { %>
           <h1>  <%= product.title %> </h1>
        <hr>
             <div>
                <img class="details__img" src=<%= product.imageURL %> > 
            </div>
        <hr>  
        <h2> <%= product.price %> $</h2>  
            <p> 
                <%=  product.description %>
            </p>
        <% } else { %>
            Didn't find a product.
        <% } %>
    </main>
</body>